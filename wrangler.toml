name = "stock-blog"
compatibility_date = "2024-01-01"

# Cloudflare Pages 빌드 설정
[build]
command = "npm run build"

# Next.js 빌드 출력 디렉토리 (.next 사용)
pages_build_output_dir = ".next"

# D1 데이터베이스 바인딩
[[d1_databases]]
binding = "DB"
database_name = "stock-blog-db"
database_id = "your-database-id-here"
